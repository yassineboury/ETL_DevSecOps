# ETL DevSecOps - Dépendances optimisées et validées LTS

# === OUTILS DE DÉVELOPPEMENT (CORE) ===
black==25.1.0           # Formatage de code (stable 2025)
isort==5.13.2           # Tri des imports  
flake8==7.1.1           # Linting
pytest==8.3.3          # Tests (LTS stable)
pytest-cov==6.0.0      # Coverage des tests
mypy==1.14.0            # Type checking (stable)
bandit==1.8.0           # Analyse sécurité

# === ETL CORE (ESSENTIELS) ===
pandas>=2.2.0           # Manipulation de données (LTS branch)
numpy>=1.26.0,<2.3.0    # Calculs numériques (compatible pandas 2.2.x)
pydantic>=2.10.0        # Validation de données
structlog>=24.4.0       # Logging structuré
requests>=2.32.0        # Client HTTP sync (APIs GitLab/SonarQube)
openpyxl>=3.1.5         # Excel export (Phase 1)
python-dateutil>=2.8.0  # Dates ISO → JJ-MM-YYYY (SPC-GEN-02)
tenacity>=8.2.0         # Retry HTTP avec jitter (SPC-GEN-05)

# === KENOBI-FORGE (ESSENTIELS) ===
jinja2>=3.1.0           # Moteur de templates
click>=8.1.0            # Interface CLI
pyyaml>=6.0             # Parsing YAML
rich>=13.9.0            # Interface CLI riche

# === SHARED UTILITIES ===
python-dotenv>=1.0.0    # Variables d'environnement

# Note: SQLAlchemy retiré - pas de DB nécessaire pour le moment
# Note: httpx retiré - ETL Phase 1 100% synchrone avec requests
# Total: 24 packages finaux (-31% vs version originale)
